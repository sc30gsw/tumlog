function preview(){
  const imageList = document.getElementById('image-list');
  document.getElementById('exercise_image').addEventListener('change', (e) => {
    console.log(123);
  });
}
window.addEventListener('load', preview);